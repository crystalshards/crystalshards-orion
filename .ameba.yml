# This configuration file was generated by `ameba --gen-config`
# on 2020-05-06 04:26:32 UTC using Ameba version 0.12.1.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Problems found: 2
# Run `ameba --only Lint/UnusedArgument` for details
Lint/UnusedArgument:
  Description: Disallows unused arguments
  IgnoreDefs: true
  IgnoreBlocks: false
  IgnoreProcs: false
  Enabled: true
  Severity: Warning
  Excluded:
  - src/models/shard.cr
  - src/require_deps.cr

# Problems found: 21
# Run `ameba --only Lint/UselessAssign` for details
Lint/UselessAssign:
  Description: Disallows useless variable assignments
  Enabled: true
  Severity: Warning
  Excluded:
  - src/models/shard.cr
  - src/manifests/shard/dependency/git.cr
  - src/controllers/home_controller.cr
  - src/controllers/authors_controller.cr
  - src/controllers/shards_controller.cr

# Problems found: 2
# Run `ameba --only Style/NegatedConditionsInUnless` for details
Style/NegatedConditionsInUnless:
  Description: Disallows negated conditions in unless
  Enabled: true
  Severity: Convention
  Excluded:
  - src/models/shard.cr

# Problems found: 1
# Run `ameba --only Style/UnlessElse` for details
Style/UnlessElse:
  Description: Disallows the use of an `else` block with the `unless`
  Enabled: true
  Severity: Convention
  Excluded:
  - src/jobs/github/project_update_queued_job.cr

# Problems found: 1
# Run `ameba --only Style/RedundantReturn` for details
Style/RedundantReturn:
  Description: Reports redundant return expressions
  AllowMultiReturn: true
  AllowEmptyReturn: true
  Enabled: true
  Severity: Convention
  Excluded:
  - src/services/github/graphql/response.cr
