#!/usr/bin/env crystal
require "clear"
require "./db/migrate/*"
require "./db/setup"

version_number = ARGV[0]?

if version_number
  Clear::Migration::Manager.instance.apply_to(version_number.to_i)
else
  Clear::Migration::Manager.instance.apply_all
end
